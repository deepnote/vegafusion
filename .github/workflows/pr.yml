name: build_test
on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]

jobs:
  build-and-test:
    uses: ./.github/workflows/build-and-test.yml
    with:
      build-all-platforms: false
